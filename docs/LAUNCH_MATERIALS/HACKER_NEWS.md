# Hacker News (news.ycombinator.com) Launch Script

## The Goal
Hacker News is the origin point of all major open-source movements. They do not care about raw politics; they care about **un-censorable architecture** and **novel engineering**. We must hook them by showing them *how* it works, not just *what* it does.

## Best Time to Post
Tuesday or Wednesday morning, exactly at **8:00 AM EST / 5:00 AM PST**.

## The Submission
**Title:** `Show HN: The Null Engine - A Sovereign Java/LLM Framework to Audit Propaganda`

**URL:** `[Link to the pseudonymous GitHub Repository]`

**The Initial Comment (Post this immediately after submitting the link):**

Hey HN,

I’ve been working on a side project that intersects OSINT, deterministic Java state machines, and local LLM inference. It’s called the **Null Engine.**

The name comes from the Null Hypothesis: the default assumption that there is no relationship between two events. In this case, the engine operates on the mathematical assumption that *all* state actors and corporate media are mutually engaged in manipulation until statistically proven otherwise. 

The core problem: Most "fact-checkers" are highly centralized and rely on cloud APIs that have incredibly strict safety filters. If you try to run deep structural motivation analysis on the Military-Industrial Complex or state energy policies, corporate APIs often decline the prompt or "hallucinate" an equivocation.

I built a native **Java 25 Engine** that functions as an "Omnidirectional Audit." 
Instead of a binary "true/false", it uses strict ontological state machines to calculate a 5-vector motivation matrix (e.g., exposing the literal financial incentives of Defense contractors alongside state sovereignty claims).

**Why I'm sharing it here:**
I specifically built a Sovereign Hardware Escape Hatch (`run-sovereign.sh`). You can run it 100% offline using `Ollama` and an uncensored local model (like `dolphin-llama3`). It totally severs the reliance on Google/Anthropic endpoints, meaning the engine can't be memory-holed or API-banned. It also features a Web3 layer that pushes the resulting forensic JSON directly to a Keccak-256 hash on EVM testnets.

I'd love your feedback on the Java execution pipeline, the local Llama integration, or the structural design of the prompt-matrix.

Repository is here: [Link]
