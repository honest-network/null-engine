# The Honest Protocol: Master Roadmap

This project is not a software service; it is a structural weapon against manufactured consensus aimed squarely at exposing the non-state entities profiting from deadlocked tragedy.
It is designed to survive its creators. This roadmap dictates the evolutionary path of the movement.

If the primary repository is seized, maintainers must fork the codebase and immediately resume development at the current active Epoch.

---

## 🟢 EPOCH 1 (Current): The Sovereign Seed
**Objective:** Perfect the offline, un-censorable execution of the Omnidirectional Audit Engine.
- `[x]` Develop a pure Java 25 Engine requiring zero external cloud dependencies.
- `[x]` Implement the **Cognitive Dehumanization Index** to mathematically identify psychological phase-3 genocidal conditioning.
- `[x]` Integrate local inference targets (Ollama / uncensored models) via `run-sovereign.sh` to construct the hardware escape hatch.
- `[ ]` **Next Step:** Port the Keccak256 Ledger client from Goerli/Sepolia testnets directly to Mainnet for immutable hashing.

## 🟡 EPOCH 2: The Browser Psychological Shield
**Objective:** Bring the protocol from the terminal directly into the cognitive frontline (the browser).
- Develop a Chrome/Brave Extension that connects to a local `run-sovereign.sh` API port.
- **The Execution:** When a user reads an article on BBC, RT, NYT, or Al Jazeera, the Honest Protocol Extension runs locally in the background. It injects warnings in red highlighted text directly over words classified by the Cognitive Dehumanization Index (e.g., "Orcs", "Cockroaches").
- The extension will spawn a sidebar showing the exact Military-Industrial Complex or Energy Oligarch motivation behind the article's specific geopolitical framing.

## 🟠 EPOCH 3: The Gamified Intelligence Bounty
**Objective:** Incentivize the mass global exposure of structural hypocrisies and shadow-motivations.
- Implement an EVM `HonestBounty` smart contract.
- Researchers ("Historians" or "Psychologists") globally crowd-source new structural hypocrisies (e.g., comparing current state territorial demands to past illegal state carvings like Kosovo).
- A decentralized set of validators verifies the historical accuracy. If passed, the JSON payload is forever embedded into the `ComparativeEngine` core, and the researcher is rewarded with crypto or un-forgeable reputation tokens.

## 🔴 EPOCH 4: The Immutable Phoenix
**Objective:** Make the network mathematically impossible to erase from human history.
- Transition from GitHub to pure P2P git hosting protocols (e.g., Radicle).
- Store all engine binaries and Intelligence Audit JSON outputs directly on the **Arweave Permaweb**.
- Ensure that even if every DNS server in a nation-state black-holes the protocol, local nodes can still sync the truth matrix via IPFS DHT routing.
